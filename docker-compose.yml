services:
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    env_file:
      - ./.env


  flask:
    build: .
    ports:
      - "80:5000"
    depends_on:
      - chroma
    env_file:
      - ./.env

volumes:
  chroma-data: